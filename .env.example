# Database Configuration
DB_PASSWORD=change_me

# JWT Configuration (Must be 256+ bits for HS256)
JWT_SECRET=change_me_to_long_secret_key_here_at_least_256_bits

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Frontend URL (used for OAuth redirects)
FRONTEND_URL=http://localhost:4200

# For Docker (these are set automatically by docker-compose)
# SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/simple_api_db
# SPRING_DATASOURCE_USERNAME=jg_admin
# SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
